// For Repo 1 Jenkinsfile
def call(Map params) {
    pipeline {
        agent any
        stages {
            stage('Test Stage') {
                steps {
                    echo "Repo 1 Jenkinsfile Loaded Successfully"
                }
            }
        }
    }
}
