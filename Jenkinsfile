// app/Jenkinsfile
def runPipeline() {
    node {
        stage('Build') {
            echo 'Building...'
        }
        stage('Test') {
            echo 'Testing...'
        }
        stage('Deploy') {
            echo 'Deploying...'
        }
    }
}

// This will be called by the main Jenkins pipeline
return this
